# DeepSeek Harness 学习路径

DeepSeek Harness（`dsh`）是 DeepSeek 的开源 agent harness。产品面是「能跑 agent 的运行时」；实现面是「一切皆插件」。底层框架是 vendored 的 [Cordis](../deepseek-harness/docs/cordis-primer.zh.md)。

仓库很大（`packages/` 下 50+ 组、上百个包），但主干很窄。**不要按目录线性读。** 先建立 6 个概念，再按下面阶段走。官方文档质量很高，本路径的工作是：排出阅读顺序、标出源码入口、标出可以先跳过的部分。

每天上课用 [course/](./course/README.md) 里的讲义（目标、讲解、实验、测验、作业）。本文是地图；讲义是教案。

## 先记住的 6 件事

1. **一切皆插件。** 模型适配器、工具注册表、会话日志、agent loop 本身都是 Cordis 插件，都可以被配置替换。没有需要打补丁的特权内核。
2. **运行中的 `dsh` 是一棵插件树。** profile（`web` / `headless`）按顺序叠加组合包（bundle），再叠用户 `cordis.patch.yml` 和 `--patch`。看真实启动树：`dsh --profile web --dump-config`。
3. **会话日志是真源。** 模型看到的历史由 `session.deriveMessages()` 从仅追加的 `SessionEvent` 投影而来。约定：**模型可见 ⟺ 已记录**。
4. **能力按 seam 替换，不按实现硬编码。** 一个 seam = Service Definition（`ctx.<key>`）+ Provider + Consumer。规范范例是 `packages/shell`：`dsh-shell` / `dsh-bash-local` / `dsh-tool-bash`。
5. **循环层级有三层。** 步骤（一次模型请求 + 其工具调用）⊂ 轮次（排空一次已接纳输入）⊂ Round（策略层迭代，如 Goal Round / Ralph Round）。不要混用这三个词。
6. **新行为挂扩展点，不改 loop。** `dsh-agent` 是接口，`dsh-agent-loop` 是默认可替换驱动器。改循环必须同步改 `docs/architecture.md`。

## 仓库地图

```
deepseek-harness/
  vendor/          Cordis 源码（先读 docs，不要先读这里）
  packages/        @deepseek-ai/dsh-* 工作区
    core/          主干：session / scope / system-prompt / tools / agent / agent-loop
    llm/           模型消息词汇 + DeepSeek 适配器
    boot/          启动粘合层（profile / bundle / dump-config）
    bundle/        发行组合包：base → web-app / headless
    shell/ fs/ …   能力 seam 族（Definition + Provider + Consumer）
    host/ client/  Web GUI 两侧（后期再读）
  apps/cli/        产品启动器 `dsh`
  examples/        可运行叶子：headless / acp / jsonrpc
  docs/            架构、教程、子系统参考、cookbook
```

读代码的固定节奏：

> 架构页 / 术语 → 子系统页 → 包 README → `src/index.ts` → 关键实现 → 测试或 snapshot

包 README 通常已经写清：角色、`ctx` 键、公开 API、已知限制。子系统页上的类型声明与源码等价（有门禁检查）。

## 阶段总览

| 阶段 | 主题 | 建议时长 | 不完成就不要往下走 |
|---|---|---|---|
| 0 | 跑起来 | 0.5–1 小时 | 能启动 Web 或看懂 `--dump-config` |
| 1 | Cordis | 1–2 天 | 能写 `apply(ctx)`，分得清 emit / waterfall / effect |
| 2 | 架构地图 | 0.5–1 天 | 能口述 turn 流程和三类事件 |
| 3 | 核心主干 | 3–5 天 | 能顺着源码走完一步 step |
| 4 | 启动与组合 | 1–2 天 | 能解释 profile / bundle / patch 叠放 |
| 5 | 规范 seam | 3–4 天 | 能按 cookbook 加一个工具 |
| 6 | 会话数据平面 | 2 天 | 懂持久化、压缩、模型可见上下文从哪来 |
| 7 | 多智能体与工作流 | 2–3 天 | 分清 subagent / goal / ralph / workflow |
| 8 | 人机交互 | 1–2 天 | 懂审批、命令、hooks、plan |
| 9 | 产品表面 | 按需 | Web / SDK / ACP / Python |
| 10 | 工程文化 | 持续 | 能按仓库约定改代码 |

---

## 阶段 0 · 跑起来

**目标：** 把它当成产品用一次，再看一眼真实插件树。不要一上来就钻 `packages/`。

### 读

- [`README.zh.md`](../deepseek-harness/README.zh.md)
- [`docs/user/guide/index.zh.md`](../deepseek-harness/docs/user/guide/index.zh.md)（Web UI）
- [`apps/cli/README.md`](../deepseek-harness/apps/cli/README.md) 前半：入口模式、profile

### 做

```sh
cd deepseek-harness
pnpm install
pnpm run build
pnpm dsh web                          # http://127.0.0.1:3080
pnpm dsh --profile web --dump-config  # 真实启动树
```

有 `DEEPSEEK_API_KEY` 时再试：

```sh
pnpm dsh --profile headless "用一句话介绍你自己"
```

### 过关问题

- `dsh web` 和 `dsh --profile headless` 差在哪一层组合包？
- `--dump-config` 里一条 entry 的 `id` / `name` / `config` 各是什么意思？

### 本阶段笔记

`learn/01-跑起来.md`：记下 home 目录（`~/.dsh`）、profile 位置、dump-config 里你认出的 5 个服务。

---

## 阶段 1 · Cordis

**目标：** 学会 harness 的编程模型。后面所有包都是这个模型的实例。

官方已经写好两条互补路径：

| 路径 | 用途 |
|---|---|
| [`docs/cordis-primer.zh.md`](../deepseek-harness/docs/cordis-primer.zh.md) | 一页概念参考 |
| [`docs/cordis-tutorial/`](../deepseek-harness/docs/cordis-tutorial/index.zh.md) | 7 章动手，无需 API key |
| [`docs/user/develop/basic/`](../deepseek-harness/docs/user/develop/basic/index.zh.md) | 把插件挂进真实 Web UI |

### 必读概念

- 插件 = 函数 `apply(ctx)`，或 `Service` 子类
- 上下文 = 服务容器，`ctx.tools` / `ctx.llm` / `ctx.sessions` 是稳定 key，不 import 具体实现
- `inject` 声明依赖；加载顺序由服务依赖决定，不是手写启动序列
- 事件四种分发：`emit` / `waterfall` / `parallel` / `serial`
- **Waterfall 必须调用 `next()` 才能委托；直接 return 会短路**
- 注册是可逆副作用：`ctx.effect()` / `ctx.on()`，卸载时撤销

### 动手顺序

1. 教程 01–04：插件、effect、服务、事件（这四章是硬前置）
2. 教程 05–06：config 与组合 / HMR
3. 教程 07：在真实 harness 上注册一个工具
4. [`user/develop/basic/index.zh.md`](../deepseek-harness/docs/user/develop/basic/index.zh.md)：`--patch` 挂本地插件到 `dsh web`

### 过关问题

- 为什么不能在 `apply` 里 `setInterval` 而不包进 `ctx.effect()`？
- waterfall 监听器忘了 `next()` 会怎样？策略门禁什么时候应该故意不调用？
- `inject: ['tools']` 的插件，在 `tools` 被卸载时会发生什么？

### 本阶段笔记

`learn/02-cordis.md`：用自己的话写 5 个核心概念 + 一张「四种分发模式」表。不要抄 primer。

---

## 阶段 2 · 架构地图

**目标：** 建立全仓心智模型。此后遇到陌生包，先回来查图，再下钻。

### 按这个顺序读，不要并行扫

1. [`docs/architecture.zh.md`](../deepseek-harness/docs/architecture.zh.md) **整篇**
2. [`docs/glossary.zh.md`](../deepseek-harness/docs/glossary.zh.md) **整篇**（术语是领域语言，后面文档默认你会用）
3. [`docs/agent-lifecycle.zh.md`](../deepseek-harness/docs/agent-lifecycle.zh.md) 时序图
4. [`docs/tool-execution-pipeline.zh.md`](../deepseek-harness/docs/tool-execution-pipeline.zh.md) 流水线图
5. [`docs/graph-atlas.zh.md`](../deepseek-harness/docs/graph-atlas.zh.md) 当作图目录，先知道有哪些图
6. [`docs/capability-seams.zh.md`](../deepseek-harness/docs/capability-seams.zh.md) 扫一眼，不必一次看完
7. [`packages/README.zh.md`](../deepseek-harness/packages/README.zh.md) 组级职责表

`module-graph.md` 很大，阶段 2 只用来确认：`agent-loop` 依赖 5 个接口服务（`agents` / `sessions` / `llm` / `tools` / `systemPrompt`），扩展插件依赖 Service Definition，不依赖具体 Provider。

### 必须能画出来的 turn 流程

```
turn/start
  领取 next-step 输入 + 一条排队消息
  组装 prompt sections + tool schemas
  -> agent/pre-step          reject | enter(messages)
     step/start
     把 enter 的消息记为 user/message
     deriveMessages() 得到模型历史
     agent/request -> llm/stream -> assistant/chunk* -> assistant/message
     tool/call* -> tools/pre-execute -> tools/execute -> tools/post-execute -> tool/result*
     step/end
     工具还欠一次请求，或又来了 next-step 输入 -> 下一步
  -> agent/turn-stopping
turn/end
```

三类事件不要混：

| 域 | 例子 | 何时用 |
|---|---|---|
| 会话事件（持久） | `turn/*` `step/*` `user/message` `assistant/*` `tool/*` | 重载后必须还在 |
| Agent 事件（实时） | `agent/pre-step` `agent/request` `agent/status` | 观察或拦截进行中的工作 |
| 能力事件 | `fs/*` `tools/*` `telemetry/*` | 给某个 seam 挂策略 / 适配器 |

`agent/pre-step`、`agent/request`、`llm/stream`、`tools/{pre-execute,execute,post-execute}` 是 waterfall；`agent/turn-stopping` 是 serial。

### 过关问题

- `followup` / `steer` / `inject` 三个入口分别进哪个 inbox？谁会唤醒驱动器？
- 为什么第一次 `pre-step` 被拒绝，仍然会留下一个不含步骤的持久轮次？
- seam 的三种角色分别是什么？为什么「只加一个工具函数」通常不够？

### 本阶段笔记

`learn/03-架构.md`：自己重画 turn 流程图，并写一张「想加 X 应该挂哪个扩展点」对照表（从 architecture 文末那张表改写）。

---

## 阶段 3 · 核心主干（最重要）

**目标：** 读完 `packages/core` + `packages/llm/llm`，能从源码跟上一次 step。这是整个仓库的脊柱。

`agent-loop` 是仓库里**唯一**包含具体循环逻辑的包。其它一切要么是抽象服务，要么是挂在扩展点上的插件。

### 阅读顺序（严格按依赖）

| 序 | 包 | 子系统页 | 源码入口 | 先搞清什么 |
|---|---|---|---|---|
| 1 | `core/scope` | [scope.zh.md](../deepseek-harness/docs/subsystems/scope.zh.md) | `packages/core/scope/src/index.ts` | 全局 vs 按 agent 作用域；shadowing；与 lineage 无关 |
| 2 | `core/session` | [session.zh.md](../deepseek-harness/docs/subsystems/session.zh.md) | `src/types.ts` → `src/surface.ts` → `src/index.ts` | `SessionEventMap`、`append`、`deriveMessages`、surface |
| 3 | `core/system-prompt` | [system-prompt.zh.md](../deepseek-harness/docs/subsystems/system-prompt.zh.md) | `src/index.ts` | section 注册、排序、`system-prompt/assemble` |
| 4 | `core/tools` | [tools.zh.md](../deepseek-harness/docs/subsystems/tools.zh.md) | `src/types.ts` → `src/schema.ts` → `src/index.ts` | `defineTool`、restrict、三条 waterfall |
| 5 | `llm/llm` | [llm-streaming.zh.md](../deepseek-harness/docs/subsystems/llm-streaming.zh.md) | `src/types.ts` → `src/message.ts` → `src/index.ts` | `Message` / `StreamChunk` / `LlmAdapter` |
| 6 | `core/agent` | [core.zh.md](../deepseek-harness/docs/subsystems/core.zh.md) | `src/types.ts` → `src/inbox.ts` → `src/index.ts` | `Agent` 句柄、`create`/`resume`、`agent/*` 词汇 |
| 7 | `core/agent-loop` | 同上 | `src/index.ts`（工厂）→ `src/agent.ts`（驱动器）→ `src/tool-calls.ts` | 真正的循环；inbox 领取；step 编排 |

每包先读 README（中文优先），再读子系统页，最后下钻 `src/`。测试目录是第二文档：`agent-loop/tests/loop.spec.ts`、`tool-calls.spec.ts`、`session/tests/session.spec.ts`。

### 关键约定（读源码时对照）

- `Agent.ctx` 上的注册既有 scope 可见性，生命周期也绑在这个 agent 上。
- `setup` 窗口只做注册，绝不驱动尚未发布的 agent。
- `followup()` 进 next-turn FIFO 并唤醒；`steer()` 进 next-step 并唤醒；`inject()` 进 next-step **不唤醒**。
- `agent` 拥有公开约定；扩展插件依赖 `dsh-agent`，不要依赖 `dsh-agent-loop`。
- 循环注入恰好 5 个服务：`agents`、`sessions`、`llm`、`tools`、`systemPrompt`。

### 动手：读一份真实 transcript

打开一份无密钥 snapshot，对照 turn 流程数事件：

- [`examples/headless-agent/tests/snapshots/advanced-toolchain/session.jsonl`](../deepseek-harness/examples/headless-agent/tests/snapshots/advanced-toolchain/session.jsonl)

按顺序标出：`turn/start` → `step/start` → `user/message` → `assistant/chunk*` → `assistant/message` → `tool/call` → `tool/result` → `step/end` → `turn/end`。

再读 [`examples/headless-agent/cordis.yml`](../deepseek-harness/examples/headless-agent/cordis.yml) 和 [`composition.md`](../deepseek-harness/examples/headless-agent/composition.md)，看最小可运行树挂了哪些插件。

### 过关问题

- `deriveMessages()` 为什么不直接返回 `session.events`？
- 为什么 UI / ACP 面向 `ctx.agents` 编程，而不是 import `ReactLoopAgent`？
- 工具 schema 是怎么进入模型请求的？（从 `ctx.tools.register` 追到 `system-prompt/assemble`）
- 一次并行工具调用在 `tool-calls.ts` 里如何保序写回 `tool/result`？

### 本阶段笔记

拆成多篇，不要挤一篇：

- `learn/04-session.md`
- `learn/05-tools-and-prompt.md`
- `learn/06-agent-loop.md`（建议手写一次 step 的伪代码）

---

## 阶段 4 · 启动与组合

**目标：** 搞清「空列表 → 运行中的产品」中间叠了什么。

### 读

1. [`packages/boot/app-boot/README.md`](../deepseek-harness/packages/boot/app-boot/README.md) 的 Profiles 一节
2. [`packages/bundle/README.md`](../deepseek-harness/packages/bundle/README.md)
3. [`packages/bundle/base/README.md`](../deepseek-harness/packages/bundle/base/README.md)（每个 profile 的第一层）
4. [`packages/bundle/web-app/README.md`](../deepseek-harness/packages/bundle/web-app/README.md)
5. [`packages/bundle/headless/README.md`](../deepseek-harness/packages/bundle/headless/README.md)
6. [`apps/cli/src/bin.ts`](../deepseek-harness/apps/cli/src/bin.ts) + [`src/args.ts`](../deepseek-harness/apps/cli/src/args.ts)
7. [`apps/cli/composition.md`](../deepseek-harness/apps/cli/composition.md)

叠放顺序（architecture 原文）：

```
空条目列表
  → profile 列出的每个 bundle（先 base，再 web-app 或 headless）
  → profile 自己的 cordis.patch.yml
  → home 级 ~/.dsh/cordis.patch.yml
  → --patch overlay
```

一条 patch 按 `id` 定位条目并**整份替换**其 config，或 `insert` 新条目。不是 deep-merge。

### 做

```sh
pnpm dsh --profile web --dump-config > /tmp/dsh-web.yml
pnpm dsh --profile headless --dump-config > /tmp/dsh-headless.yml
diff -u /tmp/dsh-web.yml /tmp/dsh-headless.yml | less
```

对照 [`docs/config-catalog.zh.md`](../deepseek-harness/docs/config-catalog.zh.md) 查你不认识的字段，不要通读目录。

### 过关问题

- 为什么改用户 patch 必须重述该条目想保留的字段？
- `!!js` 允许出现在哪些位置？为什么其它元数据必须是字面值？
- `dsh-base` 大致贡献了哪些 seam？`dsh-web-app` 在它上面加了什么？

### 本阶段笔记

`learn/07-启动组合.md`：画一张从 `dsh web` 到 `ctx.agents.create` 的启动序列。

---

## 阶段 5 · 规范 seam：先精读 shell，再仿写

**目标：** 用一个完整 seam 把「Definition / Provider / Consumer」读穿，然后自己加一个工具。

### 精读范例：`packages/shell`

```
dsh-shell            Service Definition   ctx.shell
dsh-bash-local       Provider             经 ctx.subprocess 跑本地 bash
dsh-bash-sandbox     Provider             先套 ctx.sandbox 再执行
dsh-tool-bash        Consumer             面向模型的 bash 工具
```

读：

1. [`packages/shell/README.md`](../deepseek-harness/packages/shell/README.md)
2. [`docs/subsystems/shell.zh.md`](../deepseek-harness/docs/subsystems/shell.zh.md)
3. `packages/shell/shell/src/`（request / spec 分离，这是跨包边界「显式 > 隐式」的模板）
4. `packages/shell/tool-bash/src/`
5. 顺带看一对相邻 seam：[`subprocess`](../deepseek-harness/docs/subsystems/subprocess.zh.md)、[`sandbox`](../deepseek-harness/docs/subsystems/sandbox.zh.md)、[`filesystem`](../deepseek-harness/docs/subsystems/filesystem.zh.md)

为什么 filesystem 和 subprocess 共享执行世界：把它们指向远程沙箱，Bash / PTY / LSP 会一起搬走，不必为每个提供方单独 fork。

### 再读一个适配器 seam：`llm`

- [`packages/llm/README.zh.md`](../deepseek-harness/packages/llm/README.zh.md)
- [`docs/cookbook/adding-an-llm-adapter.zh.md`](../deepseek-harness/docs/cookbook/adding-an-llm-adapter.zh.md)
- `packages/llm/llm-deepseek/src/adapter.ts`（一个完整提供方）

### 动手：写一个工具

严格按官方教程，不要自己发明形状：

1. [`docs/user/develop/basic/tool.zh.md`](../deepseek-harness/docs/user/develop/basic/tool.zh.md)
2. [`docs/cookbook/adding-a-tool.zh.md`](../deepseek-harness/docs/cookbook/adding-a-tool.zh.md)
3. [`docs/cookbook/extension-cookbook.zh.md`](../deepseek-harness/docs/cookbook/extension-cookbook.zh.md)（功能 → 机制映射表）

设计工具时一开始就要决定 UI 渲染意图（`generic` / `terminal` / `diff`）。`presentCall` / `presentResult` 必须是 `args` 的纯函数。

### 过关问题

- `ShellExecRequest` 和 `ShellExecSpec` 为什么拆开？谁负责 `resolve(request): Spec`？
- 权限拒绝应该走 `tools/pre-execute` 还是改 `tool-bash` 的 `execute`？
- 扩展插件为什么绝不能依赖 `dsh-bash-local`，只能依赖 `dsh-shell`？

### 本阶段笔记

`learn/08-capability-seam.md`：用 shell 画一张三角图，再另选一个 seam（建议 `fs` 或 `web`）自己填空。

---

## 阶段 6 · 会话数据平面

**目标：** 理解「一次对话」在磁盘、内存、模型窗口里分别长什么样。

| 主题 | 文档 | 包 |
|---|---|---|
| 持久化 | [persistence.zh.md](../deepseek-harness/docs/subsystems/persistence.zh.md) | `session/session-persistence*` |
| 投影 | [session-projection.zh.md](../deepseek-harness/docs/subsystems/session-projection.zh.md) | `session/session-projection` |
| 压缩 | [compaction.zh.md](../deepseek-harness/docs/subsystems/compaction.zh.md) | `compaction/` |
| 请求上下文 | [context 组 README](../deepseek-harness/packages/context/README.md) | `context/`（workspace 指令、时间） |
| 检索 | [session-query.zh.md](../deepseek-harness/docs/subsystems/session-query.zh.md) | `session-query/` |
| 标题 | [session-title.zh.md](../deepseek-harness/docs/subsystems/session-title.zh.md) | `session/session-title*` |

压缩挂在 `agent/pre-step`（压力）和 `agent/request-error`（上下文溢出），不进 loop 本体。读 lifecycle 文档后半关于 compaction 的段落。

对照 snapshot：[`examples/headless-agent/tests/snapshots/compaction-recovery/`](../deepseek-harness/examples/headless-agent/tests/snapshots/compaction-recovery/)。

### 过关问题

- JSONL 和 SQLite 后端各自承担什么？崩溃恢复靠哪条路径？
- 新增一种模型可见输入，为什么必须同时扩 `SessionEventMap`？
- compaction 改写的是原始日志，还是 surface？

### 本阶段笔记

`learn/09-会话数据平面.md`

---

## 阶段 7 · 多智能体与工作流

**目标：** 分清四条「让 agent 继续干活」的机制。它们名字相近，语义完全不同。

| 机制 | 是什么 | 不是什么 | 入口 |
|---|---|---|---|
| **subagent** | 把一个轮次委派给另一个 agent / 产品 | 不是同会话目标 | [subagent.zh.md](../deepseek-harness/docs/subsystems/subagent.zh.md) |
| **goal** | 附着在现有会话上的持久完成目标 | 不是调度器，也不是新对话 | [goal.zh.md](../deepseek-harness/docs/subsystems/goal.zh.md) |
| **Ralph** | 面向不可变目标的前台全新 agent 循环；跨 Round 靠交接 + 共享工作区 | 不是同会话 goal，也不是通用 workflow | glossary「Ralph」 |
| **workflow** | worker-thread 工作流引擎 + `workflow` 工具 | 不是 Ralph 本身 | [workflow.zh.md](../deepseek-harness/docs/subsystems/workflow.zh.md) |

再读：

- [`preset/`](../deepseek-harness/packages/preset/README.md)：按会话用 `cordis.yml` 组装 agent（不同能力集合）
- [`jobs/`](../deepseek-harness/docs/subsystems/jobs.zh.md)：后台任务 + `job_*` 工具
- snapshot：[`examples/headless-agent/tests/snapshots/ralph-loop/`](../deepseek-harness/examples/headless-agent/tests/snapshots/ralph-loop/)
- snapshot：[`examples/headless-agent/tests/snapshots/subagent-settlement/`](../deepseek-harness/examples/headless-agent/tests/snapshots/subagent-settlement/)

scope **不**向子 agent 继承。子树关系只活在 lineage 数据里（`parentSession`、`delegationDepth`）。这是最容易读错的一点。

### 过关问题

- 父 agent 上注册的工具，子 agent 看得到吗？
- Goal Round 和会话里一次普通人类轮次有何区别？
- Ralph Round 为什么不接收父会话的对话种子？

### 本阶段笔记

`learn/10-多智能体.md`：用一张表对比 subagent / goal / ralph / workflow / jobs。

---

## 阶段 8 · 人机交互平面

**目标：** 搞清「人类」和「模型」走的是两条完全不同的通道。

| 通道 | 机制 | 文档 |
|---|---|---|
| 人类斜杠命令 | `ctx.commands`，不进模型消息 | [commands.zh.md](../deepseek-harness/docs/subsystems/commands.zh.md) |
| 一次性审批 | `ctx.approval`，常从 `tools/pre-execute` 返回 `ask` | [approval.zh.md](../deepseek-harness/docs/subsystems/approval.zh.md) |
| 模型向用户提问 | `ctx.askUser` / ask 工具 | [user-questions.zh.md](../deepseek-harness/docs/subsystems/user-questions.zh.md) |
| 权限预设 | permission presets | [permission-presets.zh.md](../deepseek-harness/docs/subsystems/permission-presets.zh.md) |
| Plan 模式 | 仅记日志的协作状态 + 经评审退出 | [plan.zh.md](../deepseek-harness/docs/subsystems/plan.zh.md) |
| 外部钩子桥 | Claude Code / Codex hooks → 原生扩展点 | `packages/hooks/` |

`packages/interaction/` 是这一平面的组入口。extension-cookbook 里的权限门禁示例值得手抄一遍。

### 过关问题

- `/goal` 为什么不是一个模型工具？
- 命令输出默认为什么只是 UI 状态？什么时候才会变成持久领域事实？

### 本阶段笔记

`learn/11-人机交互.md`

---

## 阶段 9 · 产品表面（按需，不要提前）

主干通了再选一条表面往下走。`packages/client` 有 1000+ 文件，**不要作为学习入口**。

| 表面 | 何时读 | 入口 |
|---|---|---|
| Web GUI | 要改 Chat / Host | `packages/host` + `packages/client` 的组 README；[web-server.zh.md](../deepseek-harness/docs/subsystems/web-server.zh.md)、[client-modules.zh.md](../deepseek-harness/docs/subsystems/client-modules.zh.md)、[cookbook/adding-a-conversation-node](../deepseek-harness/docs/cookbook/adding-a-conversation-node.zh.md) |
| TypeScript SDK | 进程外驱动 | `packages/sdk` + `examples/jsonrpc-agent` |
| ACP | 编辑器 / 自动化客户端 | `packages/acp` + `examples/acp-agent` |
| Python SDK | 非 TS 宿主 | `python/README.zh.md`、[user/guide/python-sdk](../deepseek-harness/docs/user/guide/python-sdk.zh.md) |
| Typert / API 网关 | 远程 RPC / BFF | [api-gateway.zh.md](../deepseek-harness/docs/api-gateway.zh.md)、[typert.zh.md](../deepseek-harness/docs/subsystems/typert.zh.md) |

自指演示（agent 改自己的插件树）：`pnpm run demo:cordis`，配置在 `examples/web-cordis/`。这是理解「运行时就是插件树」的最好实验，但需要 API key。

---

## 阶段 10 · 工程文化（边做边读）

准备改代码时再读，但改之前必须读：

1. 根 [`AGENTS.md`](../deepseek-harness/AGENTS.md) 的 Conventions
2. [`docs/defensive-patterns.zh.md`](../deepseek-harness/docs/defensive-patterns.zh.md)（生命周期、并发、子进程、teardown）
3. [`docs/testing.zh.md`](../deepseek-harness/docs/testing.zh.md)
4. [`docs/development.zh.md`](../deepseek-harness/docs/development.zh.md) 里与你有关的小节
5. 一篇 postmortem，感受真实翻车形状：[`docs/postmortem/`](../deepseek-harness/docs/postmortem/README.zh.md)

几条会反复撞上的规矩：

- 注册必须走 `ctx.effect()` / `ctx.on()`；`register()` 返回 disposer
- waterfall 监听器必须 `next()`
- 品牌化 id（`Branded<B>`），跨边界不用裸 `string`
- 包边界上 defaulting 是显式的 `resolve(request): Spec`，不是 `run()` 里的 `??`
- 可部署旋钮必须是 `Config` 字段，禁止插件里写死 `DEFAULT_*` 当配置
- 非平凡改动要在同一 PR 写 Agent Note；产品可见行为要加 keyless snapshot
- `test:coverage` 才是 CI 覆盖率门禁（`packages/*/*/src` 逐文件 100%）

---

## 先不要读什么

这些东西不是不重要，而是作为入口会把你淹没：

| 推迟 | 原因 |
|---|---|
| `packages/client/**` | 浏览器半侧，体积最大，依赖主干事件流 |
| `docs/config-catalog.md` / `tool-catalog.md` 通读 | 生成目录，当字典查 |
| `docs/i18n/`、`*.i18n.yaml` | 文档工艺，与运行时无关 |
| `vendor/cordis` 源码 | 先把 primer + tutorial 吃透；源码是第三遍 |
| `native/landlock-run` | 只在做 Linux 沙箱后端时需要 |
| `packages/typert` 生成器实现 | 先会用网关，再看生成器 |
| `.agents/notes/archived/` | 已冻结历史，不是现行权威 |
| `scripts/` 门禁实现 | 改文档/仓库工艺时再看 |

现行设计决策在 `.agents/notes/implemented/`。阶段 3 以后，读包 README 里链过去的那几篇就够，不要浏览整个 notes 树。

---

## 一条最短路径（如果只想先「读懂」）

按周压缩，大约 8 个核心工作日 + 4 天进阶。**每天的讲义在 [course/](./course/README.md)**，不要只按下面这张表自己摸。

```
第 1 天    course/day-01.md   跑起来 + Cordis 01–04
第 2 天    course/day-02.md   Cordis 05–07 + 挂插件到 web
第 3 天    course/day-03.md   架构 + 术语 + 生命周期
第 4 天    course/day-04.md   scope + session
第 5 天    course/day-05.md   system-prompt + tools
第 6 天    course/day-06.md   llm + agent + agent-loop
第 7 天    course/day-07.md   启动与组合
第 8 天    course/day-08.md   shell seam + 写工具
第 9 天    course/day-09.md   会话数据平面（进阶）
第 10 天   course/day-10.md   多智能体与工作流（进阶）
第 11 天   course/day-11.md   人机交互（进阶）
第 12 天   course/day-12.md   产品表面 + 工程文化（进阶）
```

## 笔记约定

每阶段结束写一篇自己的话，建议固定四个小节：

```md
# <主题>

## 它是什么
## 关键对象与事件
## 源码入口（path:symbol）
## 我仍然不清楚的问题
```

术语跟 [`docs/glossary.zh.md`](../deepseek-harness/docs/glossary.zh.md) 走：说 scope 就说 scope，不要自造「命名空间」「沙箱上下文」。说轮次 / 步骤 / Round 时保持三分。

## 官方文档角色对照

本路径引用官方文档时，它们的职责是：

| 官方文档 | 角色 |
|---|---|
| `docs/architecture.zh.md` | 跨子系统行为（必读） |
| `docs/glossary.zh.md` | 领域语言（必读） |
| `docs/cordis-tutorial/` | Cordis 动手 |
| `docs/subsystems/*.zh.md` | 单子系统词汇与接线（查） |
| `docs/cookbook/` | 扩展怎么写 |
| `docs/user/develop/` | 面向插件作者的产品教程 |
| 包 README | 该包的用途、API、限制 |
| `docs/graph-atlas.zh.md` | 所有图的索引 |
